#include <stdio.h>

/**
 * main - lowercase alphabets entry point
 * Return: Always 0
 */
int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a')
		{
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}

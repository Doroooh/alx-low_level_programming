#include "main.h"
#include <stdio.h>

/**
 * mul - multiples two integers
 * @c: first integer
 * @b: second integer
 * Return: zero always
 */

int mul(int c, int b)
{
	int result = c * b;

	return (result);
}
